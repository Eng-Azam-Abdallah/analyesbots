# Analyes — ذكاء السوق والمبيعات والفئات

> وثيقة تحليل ومتطلبات لميزة: **المبيعات / النشاط / التوفر / الفئات**.  
> التاريخ: 2026-08-06  
> الحالة: **تم التنفيذ** (مراحل 0–4)  
> مرتبطة بـ: [`REQUIREMENTS.md`](./REQUIREMENTS.md)

---

## 1. ماذا تريد بالضبط؟ (فهم الطلب)

طلبك يتكوّن من **أربعة أسئلة سوقية** + **تنظيم بصري**:

| # | السؤال بلغة صاحب المشروع | المعنى التحليلي |
|---|--------------------------|-----------------|
| 1 | أي بوت أكثر مبيعًا؟ | ترتيب المصادر حسب حجم البيع / النشاط |
| 2 | أي منتج أكثر مبيعًا عند كل البوتات؟ | المنتج (أو عائلة المنتج) الأقوى عبر السوق كله |
| 3 | كل بوت كم باع اليوم؟ وبكم باع؟ | تقرير يومي: **كمية** + **قيمة تقديرية (USDT)** لكل بوت |
| 4 | أي منتج أكثر توفرًا في السوق؟ (GPT / Claude / Netflix…) | أين المخزون الأكبر / الأوسع انتشارًا عبر البوتات |
| 5 | تقسيم المنتجات لفئات (GPT، Claude، …) | صفحة كتالوج حسب **عائلة منتج** تجمع كل الاشتراكات التابعة |

هذا ليس شراءً عبر المنصة؛ الهدف **قراءة السوق واتخاذ قرار** بسرعة.

---

## 2. الحقيقة المهمة قبل التصميم

### 2.1 ماذا نملك اليوم؟

النظام الحالي يراقب:

- السعر الحالي + لقطات السعر
- الكمية (`stock`) + تغيّرات الكمية (`stock_up` / `stock_down`)
- ظهور/اختفاء المنتج (`new` / `gone`)
- رصيد API إن وُجد

**لا نخزّن اليوم رقم مبيعات يومي رسمي** من أغلب البوتات.

### 2.2 ماذا تُرجع الـ APIs عن «البيع»؟

| المصدر | هل فيه رقم مبيعات؟ | ملاحظات |
|--------|---------------------|---------|
| أغلب Reseller / Buyer APIs | لا | فقط سعر + كمية |
| Qamify | جزئيًا | `sold_total` تراكمي على المنتج (مفيد لكن ليس «بيع اليوم» مباشرة) |
| باقي المصادر الحالية | غالبًا لا | أي «مبيعات يومية» يجب **استنتاجها** |

### 2.3 الخلاصة الحاسمة

> **لا نقدر ندّعي «مبيعات حقيقية مؤكدة» لكل البوتات** لأن أغلب الـ APIs لا تعطي جدول أوامر يومي.  
> نقدر نبني **مؤشرات سوق قوية (Proxies)** من تغيّر المخزون، ونفصل في الواجهة بين:
>
> 1. **مبيعات مستنتجة (من انخفاض المخزون)** — متاحة لكل المصادر تقريبًا  
> 2. **مبيعات معلنة من المصدر** — إن وُجد حقل مثل `sold_total`  
> 3. **التوفر / السيولة** — كمية حالية + عدد البوتات التي تعرض المنتج

بدون هذا الفصل، الأرقام ستكون مضلّلة.

---

## 3. تعريف المقاييس (Glossary)

### 3.1 مبيعات مستنتجة (Inferred Sales / Velocity)

عند كل مزامنة ناجحة، إذا انخفضت كمية منتج:

```text
qty_sold_proxy = max(0, stock_before - stock_after)
revenue_proxy  = qty_sold_proxy × current_price
```

- تُحسب فقط عند حدث `stock_down` (أو فرق لقطات الكمية).
- **لا** تُحسب عند ارتفاع الكمية (إعادة تزويد).
- **لا** تُحسب عند اختفاء المنتج (`gone`) تلقائيًا كبيع كامل — إلا بقاعدة اختيارية لاحقًا (محفوفة بالمخاطر).

**حدود المؤشر:**

- إذا البوت لا يحدّث `stock` بدقة → المؤشر ضعيف.
- إذا `stock = null` دائمًا أو ثابت → لا مبيعات مستنتجة.
- إعادة التعبئة ثم البيع السريع بين دقيقتين قد تُفقد أو تُقلَّل.

رغم ذلك، هذا أفضل تقدير عملي عبر مصادر متعددة بدون API أوامر.

### 3.2 مبيعات معلنة (Declared Sales)

إن وُجد في `rawPayload` حقل مثل:

- `sold_total`
- `your_sold_qty`

نحفظه كسلسلة زمنية، ونحسب:

```text
declared_sold_delta = sold_total_now - sold_total_prev
```

هذا أدق **عندما يوجد**، لكنه غير متوفر عند كل بوت.

### 3.3 التوفر / «أكثر تزفر» (Market Availability)

ثلاثة مقاييس منفصلة (لا تخلطها مع المبيعات):

| المقياس | المعنى |
|---------|--------|
| **Total Stock** | مجموع الكمية لكل منتجات العائلة عبر البوتات |
| **Coverage** | كم بوت يعرض العائلة الآن |
| **Median Price** | السعر الوسيط للعائلة (مقارنة سريعة) |

«أكثر تزفر» = غالبًا أعلى **Total Stock** و/أو أعلى **Coverage**، مع فلتر عائلة (GPT / Claude / Netflix…).

### 3.4 نشاط البوت اليومي (Bot Daily Activity)

لكل بوت ولكل يوم (UTC أو Asia/Riyadh — يُثبت لاحقًا):

| الحقل | الحساب |
|-------|--------|
| `units_proxy` | مجموع `qty_sold_proxy` |
| `revenue_proxy` | مجموع `revenue_proxy` |
| `stock_down_events` | عدد أحداث انخفاض الكمية |
| `active_skus` | عدد منتجات مختلفة تحرّكت |
| `declared_units` | إن وُجدت مبيعات معلنة |

ترتيب «أكثر بوت مبيعًا» يعتمد افتراضيًا على **`revenue_proxy`** مع إمكانية التبديل إلى **`units_proxy`**.

---

## 4. الفئات / عائلات المنتجات (Product Families)

### 4.1 لماذا صفحة وحدها؟

**نعم — صفحة مستقلة أفضل.**

| الخيار | التقييم |
|--------|---------|
| دمج الفئات داخل `/products` فقط | يزدحم الفلتر ويضعف سؤال «من الأقوى في السوق؟» |
| بطاقات صغيرة في اللوحة فقط | مفيد كملخص، غير كافٍ للاستكشاف |
| **صفحة `/categories` مستقلة** | الأنسب: عائلة → منتجات مجمّعة من كل البوتات → أسعار/كميات/نشاط |

**التوصية:**

1. **`/categories`** — كتالوج العائلات (GPT، Claude، Netflix، Canva، …)  
2. **`/categories/[slug]`** — تفاصيل عائلة واحدة + مقارنة البوتات  
3. **ملخص في اللوحة `/`** — Top 5 عائلات توفرًا + Top بوتات نشاطًا اليوم  
4. **`/analytics` أو قسم في اللوحة** — تقارير اليوم/7 أيام (مبيعات مستنتجة)

### 4.2 كيف نقسم؟ (قواعد التصنيف)

التصنيف **قائم على قواعد + قاموس كلمات مفتاحية** أولًا (سريع وقابل للضبط)، مع إمكانية ML لاحقًا.

أمثلة قواعد أولية (عربي/إنجليزي):

| العائلة (`slug`) | كلمات دالة في العنوان/الوصف |
|------------------|------------------------------|
| `chatgpt` | chatgpt, chat gpt, gpt-4, gpt4, openai, plus fw |
| `claude` | claude, anthropic |
| `gemini` | gemini, google ai, ai pro |
| `netflix` | netflix |
| `canva` | canva |
| `cursor` | cursor |
| `midjourney` / `leonardo` / `runway` | حسب الاسم |
| `spotify` | spotify |
| `adobe` | adobe, creative cloud |
| `other` | كل ما لا يطابق قاعدة |

قواعد إضافية مقترحة:

- مدة الاشتراك تُستخرج كوسم فرعي إن أمكن: `1m` / `3m` / `12m` / `18m` (Regex بسيط).
- لا نعتمد على `category` القادم من API وحدها — تختلف بين البوتات وغير موحّدة.
- كل منتج يحصل على: `familySlug` + `familyLabelAr` + `confidence` (high/medium/low).
- المنتجات منخفضة الثقة تذهب إلى **«غير مصنّف»** للمراجعة اليدوية لاحقًا.

### 4.3 ماذا يرى المستخدم في صفحة الفئة؟

لصفحة `GPT` مثلًا:

- عدد العروض النشطة عبر البوتات
- أرخص سعر / أعلى سعر / وسيط
- مجموع المخزون
- أكثر بوت توفرًا لهذه العائلة
- قائمة كل الاشتراكات (ChatGPT Plus 1M، FW، Team، …) مرتبة بالسعر أو بالنشاط
- نبض آخر 24 ساعة: انخفاضات كمية مستنتجة داخل العائلة

---

## 5. تصميم البيانات المقترح

### 5.1 حقول على `Product`

```text
familySlug        String?   // chatgpt | claude | ...
familyLabel       String?   // ChatGPT
familyConfidence  String?   // high | medium | low
durationTag       String?   // 1m | 3m | 12m | 18m | unknown
soldTotal         Int?      // إن أعلنه المصدر (مثل Qamify)
```

### 5.2 جدول جديد: `SalesProxyEvent` (حدث بيع مستنتج)

يُنشأ عند انخفاض كمية موثوق:

```text
id
productId
botId
capturedAt
qty            // stock_before - stock_after
unitPrice      // سعر لحظة الحدث
revenue        // qty * unitPrice
source         // "stock_delta" | "declared_delta"
fromStock
toStock
```

### 5.3 جدول تجميعي يومي (اختياري لكن مفيد للأداء): `DailyBotStats`

```text
botId
day            // date (timezone مثبت)
unitsProxy
revenueProxy
stockDownCount
declaredUnits
```

يُحدَّث بعد كل مزامنة أو عبر job تجميعي كل ساعة.

### 5.4 جدول تجميعي عائلي يومي (اختياري): `DailyFamilyStats`

```text
familySlug
day
unitsProxy
revenueProxy
totalStock
botsCovering
```

### 5.5 لماذا لا نكتفي بـ `MarketChange`؟

`MarketChange` موجود ويعمل للنبض، لكن تقارير المبيعات تحتاج:

- كمية مباعة صريحة
- إيراد تقديري
- تجميع يومي سريع بدون إعادة حساب كل التاريخ في كل طلب

يمكن في **المرحلة 1** الحساب من `MarketChange` حيث `kind = stock_down` و`fromStock/toStock` موجودان، ثم الانتقال لجدول مخصّص إذا أصبح بطيئًا.

---

## 6. واجهات API المقترحة (Backend)

| Method | Path | الغرض |
|--------|------|-------|
| `GET` | `/analytics/bots/daily?day=YYYY-MM-DD` | كل بوت: كمية/إيراد مستنتج لليوم |
| `GET` | `/analytics/bots/ranking?range=7d` | ترتيب البوتات الأكثر نشاطًا |
| `GET` | `/analytics/products/top?range=7d` | أكثر المنتجات/العائلات مبيعًا (proxy) |
| `GET` | `/analytics/availability?family=chatgpt` | التوفر لعائلة أو للكل |
| `GET` | `/categories` | قائمة العائلات + ملخص |
| `GET` | `/categories/:slug` | تفاصيل عائلة + العروض من كل بوت |
| `POST` | `/categories/reclassify` | إعادة تشغيل قواعد التصنيف (يدوي/cron) |

كل الأرقام يجب أن تحمل في الـ JSON:

```json
{
  "metricType": "inferred_from_stock",
  "disclaimer": "تقدير من انخفاض المخزون — ليس تقرير مبيعات رسمي من البوت"
}
```

حتى لا يُفهم الرقم كحقيقة محاسبية.

---

## 7. الواجهة — هيكل الصفحات المقترح

```text
لوحة السوق (/)
  ├─ بطاقات اليوم: أكثر بوت نشاطًا | أكثر عائلة مبيعًا (proxy) | أكثر عائلة توفرًا
  └─ روابط سريعة للتحليل والفئات

المصادر (/bots)
  └─ يضاف عمود/بطاقة: نشاط اليوم (units + revenue proxy)

المنتجات (/products)
  └─ فلتر عائلة + وسم المدة

الفئات (/categories)          ← صفحة جديدة مستقلة
  ├─ شبكة عائلات
  └─ /categories/[slug]       ← مقارنة عروض البوتات داخل العائلة

التحليل (/analytics)          ← صفحة جديدة مستقلة (موصى بها)
  ├─ تبويب: البوتات (يومي / 7 أيام)
  ├─ تبويب: المنتجات الأكثر مبيعًا (proxy)
  └─ تبويب: الأكثر توفرًا
```

### لماذا فصل `/analytics` عن `/categories`؟

- **الفئات** = استكشاف كتالوج موحّد (ماذا يوجد؟ أين أرخص؟ أين أكثر كمية؟)  
- **التحليل** = زمن + ترتيب + «من باع أكثر اليوم؟»

خلط الاثنين في صفحة واحدة يُربك السؤال الواحد لكل شاشة.

---

## 8. خطة تنفيذ مرحلية (موصى بها)

### المرحلة A — أساس التصنيف والتوفر (قيمة سريعة)

1. محرّك قواعد `familySlug` عند المزامنة + أمر إعادة تصنيف.
2. صفحة `/categories` + `/categories/[slug]`.
3. في اللوحة: أكثر العائلات توفرًا (Total Stock + Coverage).

**النتيجة:** تجيب سؤال «أكثر تزفر» + تنظيم GPT/Claude/… بدون انتظار مبيعات كاملة.

### المرحلة B — المبيعات المستنتجة اليومية

1. حساب `qty/revenue` من انخفاض المخزون عند كل sync (أو من `MarketChange`).
2. `GET /analytics/bots/daily` + ترتيب البوتات.
3. صفحة `/analytics` تبويب البوتات والمنتجات.

**النتيجة:** تجيب «أي بوت أكثر مبيعًا» و«كم باع اليوم / بكم» كتقدير واضح ومعلَّم.

### المرحلة C — المبيعات المعلنة عند توفرها

1. استخراج `sold_total` (وأي حقول مشابهة) من المصادر التي تدعمها.
2. عرض عمود منفصل «معلن من المصدر» بجانب «مستنتج».

### المرحلة D — تحسينات

1. اختيار المنطقة الزمنية لليوم (مثلاً `Asia/Riyadh`).
2. tunning لقواعد التصنيف من واجهة بسيطة أو ملف إعداد.
3. تنبيه: عائلة ارتفع نشاطها فجأة / بوت توقّف عن انخفاض المخزون.

---

## 9. قرارات يجب تثبيتها معك قبل التنفيذ

| قرار | الخيارات | توصيتي |
|------|----------|--------|
| مصدر رقم «المبيعات» | مستنتج / معلن / الاثنان | **الاثنان** مع تسمية واضحة |
| ترتيب البوت الافتراضي | كمية أو مبلغ | **المبلغ (revenue_proxy)** |
| يوم الإحصاء | UTC أو الرياض | **Asia/Riyadh** |
| هل `gone` = بيع؟ | نعم / لا | **لا** في المرحلة الأولى |
| صفحة الفئات | مستقلة / داخل المنتجات | **مستقلة `/categories`** |
| صفحة التحليل | مستقلة / داخل اللوحة فقط | **مستقلة `/analytics` + ملخص في اللوحة** |

---

## 10. مخاطر وتحليل صحيح (حتى لا نخدع أنفسنا)

1. **مخزون وهمي أو غير محدّث** عند بعض البوتات → المبيعات المستنتجة تكون صفر أو مبالغ فيها. الحل: إظهار جودة المؤشر لكل بوت (مثلاً نسبة المنتجات ذات `stock` الرقمي المتغيّر).
2. **إعادة تزويد بين دقيقتين** تخفي مبيعات. الحل لاحقًا: زيادة تكرار المزامنة لمصادر معيّنة أو قبول هامش الخطأ.
3. **نفس المنتج بأسماء مختلفة** عبر البوتات. الحل: العائلات بالقواعد، ثم تحسين يدوي للقواعد.
4. **خلط التوفر مع المبيعات.** منتج كثير المخزون قد يكون راكدًا؛ منتج قليل المخزون قد يكون سريع البيع. لذلك صفحتان/تبويبان منفصلان.
5. **عملات مختلفة.** التوحيد الافتراضي USDT كما هو الآن؛ أي عملة أخرى تُوسم ولا تُخلط بلا تحويل.

---

## 11. ملخص تنفيذي لصاحب المشروع

أنت تريد معرفة:

1. **من البوت النشيط في البيع اليوم؟** → ترتيب يومي من انخفاض المخزون (+ معلن إن وُجد).  
2. **ما المنتج/العائلة الأكثر بيعًا؟** → تجميع المبيعات المستنتجة على مستوى العائلة والمنتج.  
3. **ما الأكثر توفرًا في السوق؟** → مجموع المخزون + عدد البوتات لكل عائلة (GPT/Claude/Netflix…).  
4. **تنظيم السوق** → صفحة فئات وحدها تجمع كل اشتراكات GPT معًا وهكذا.

**الوضع الحالي للمنصة لا يعطي مبيعات رسمية يومية من كل API**، لذلك التصميم الصحيح هو:

- بناء **تقدير مبيعات من حركة الكمية** بشفافية  
- بناء **فئات سوقية** قوية  
- فصل صفحة **الفئات** عن صفحة **التحليل**

بعد موافقتك على قرارات القسم 9، يمكن تنفيذ المرحلة A ثم B مباشرة على المحلي ثم رفعها للسيرفر.

---

## 12. حالة التنفيذ (2026-08-06)

| مرحلة | المحتوى | الحالة |
|--------|---------|--------|
| 0 | Prisma (`family*` + `SalesProxyEvent`) + مصنّف العائلات + ربط `stock_down` | تم |
| 1 | `/categories` API/UI + ملخص اللوحة + رابط AppShell | تم |
| 2 | `/analytics` يومي + ترتيب + backfill من `MarketChange` | تم |
| 3 | `soldTotal` معلن (Qamify) منفصل عن المستنتج | تم |
| 4 | ضبط قواعد التصنيف + شارة جودة المؤشر + نشر | تم |

**واجهات:** `/categories`، `/categories/[slug]`، `/analytics`  
**API:** `GET /categories`، `POST /categories/reclassify`، `GET /analytics/bots/daily`، `…/ranking`، `…/products/top`، `…/availability`، `POST /analytics/backfill`  
**يوم الإحصاء:** `Asia/Riyadh` — `metricType: inferred_from_stock` مع تنويه ثابت.

### خارج النطاق الحالي (لاحقًا)
تنبيهات تليجرام، اعتبار `gone` بيعًا، تحويل عملات، UI لتحرير قواعد التصنيف يدويًا.
